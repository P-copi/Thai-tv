name: Build Thai TV Player NEW

on:
  workflow_dispatch:
  push:
    paths:
      - 'ThaiTVPlayer-New/**'
      - '.github/workflows/build-thai-tv-new.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ThaiTVPlayer-New
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        run: gradle assembleDebug --no-daemon
      - name: Verify APK
        run: test -s app/build/outputs/apk/debug/app-debug.apk
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ThaiTVPlayer-New-debug
          path: ThaiTVPlayer-New/app/build/outputs/apk/debug/app-debug.apk

# Build request refreshed
